export enum OrderState {
  POSTED = "POSTED",
  ASSIGNED = "ASSIGNED",
  PURCHASED = "PURCHASED",
  COMPLETED = "COMPLETED",
  EXPIRED = "EXPIRED",
}

export enum AmountType {
  MASS = "MASS",
  PIECE = "PIECE",
  LENGTH = "LENGTH",
  AREA = "AREA",
}
